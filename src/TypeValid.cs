using System;
using System.Collections.Generic;
using System.Text;

namespace ValidBox
{
    public enum TypeValid
    {
        Numeric, //Número entero sin signo
        SNumeric, //Número entero con signo
        Letter, //Letras
        Decimal, //Decimal sin signo
        SDecimal //Decimal con signo
    }
}
